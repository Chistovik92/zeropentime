# zeropentime — дорожная карта по версиям

Как устроено: [ARCHITECTURE.md](ARCHITECTURE.md). Технические решения и задачи по фазам: [PLAN.md](PLAN.md). Здесь — **что и в какой версии получает пользователь** и когда версия считается готовой.

**Релиз выходит на каждую версию третьего уровня (x.y.z).** Минорная версия (x.y) — это веха из нескольких патч-релизов. Каждый патч-релиз — законченный и проверенный шаг, который можно поставить и использовать. Правила — в конце документа.

Сроки — оценка для 1–2 разработчиков.

## Обзор вех

| Веха | Название | Главное для пользователя | Релизы | Срок | Статус |
|---|---|---|---|---|---|
| **0.1** | Основа | Комнаты на AmneziaWG 3, контроллер с админ-панелью, приглашения | 0.1.0 – 0.1.3 | 1–2 нед | ✅ 2026-09-26 |
| **0.2** | Связь отовсюду | Работа за любым NAT, relay, VLESS при блокировке UDP, LAN-игры | 0.2.0 – 0.2.3 | 4–5 нед | ✅ 2026-09-26 |
| **0.3** | Интернет через комнату | Exit-узлы, kill switch, свой DNS, управление из терминала, доступ к домашней сети | 0.3.0 – 0.3.6 | 4 нед | ✅ 2026-09-26 |
| **0.4** | Порядок в сети | DNS-имена, правила доступа, бэкап ключей | 0.4.0 – 0.4.2 | 3 нед | 🟡 выпущен 0.4.1, дальше 0.4.2 |
| **0.5** | Без центра | Работа неделями без контроллера, gossip, DHT | 0.5.0 – 0.5.2 | 4 нед | ⚪ |
| **0.6** | Для людей | Установщики, трей-клиент, автообновление, 2FA/OIDC | 0.6.0 – 0.6.4 | 4 нед | ⚪ |
| **1.0** | Стабильная | Заморозка протокола, аудит, нагрузочные цели | 1.0.0-rc.N → 1.0.0 | 3–4 нед | ⚪ |
| 1.1+ | Платформы | macOS, Android, iOS, режим L2 | 1.1.0, … | по запросу | ⚪ |

Итого до 1.0 — около 6 месяцев, 25+ релизов.

---

## Требует ручной проверки

Решение от 2026-09-26: релизы выходят без проверки на реальных машинах (в этой среде её не провести), а непроверенное копится здесь и в разделе «Как проверено» заметок к релизу.

- [x] Linux ↔ Linux на настоящих TUN-интерфейсах, в том числе через конусные и симметричные NAT — автоматически в CI (NAT-стенд, с 0.2.1);
- [ ] Windows ↔ Linux в одной LAN: ping, SMB, исключение из панели ≤ 5 с (с 0.1.0);
- [ ] то же через интернет с пробросом порта (с 0.1.0);
- [ ] профиль сети «Частная» включается сам (с 0.1.2);
- [ ] docker-compose с реальным доменом и сертификатом Let's Encrypt (с 0.1.3);
- [ ] проброс порта на реальных роутерах: UPnP и NAT-PMP (с 0.2.0);
- [x] broadcast и mDNS между узлами на настоящих TUN — в CI (Linux, с 0.2.3);
- [ ] broadcast и LAN-игры на Windows (с 0.2.3);
- [x] subnet router на Linux — в CI (с 0.3.0);
- [ ] маршруты в сеть за узлом на Windows и macOS (с 0.3.0);
- [x] exit-узел и выход через него на Linux — в CI (с 0.3.1);
- [ ] exit-узел с реальными провайдерами и брандмауэрами дистрибутивов (с 0.3.1);
- [x] DNS через exit и закрытие IPv6 на Linux — в CI, кроме настройки systemd-resolved (с 0.3.2);
- [ ] exit на клиенте Windows: маршруты, привязка сокетов, NRPT, тест на утечку DNS (с 0.3.2);
- [ ] DNS через exit с systemd-resolved на реальном Linux (с 0.3.2);
- [x] kill switch на Linux — в CI (с 0.3.3);
- [ ] kill switch на Windows (WFP) (с 0.3.3);
- [ ] свой DNS в ОС с несколькими серверами: systemd-resolved и NRPT (с 0.3.4);
- [x] zpt status / peers на Linux — в CI (с 0.3.5);
- [ ] служба Windows (`zpt service`), журнал в файле и канал управления на Windows (с 0.3.5);
- [ ] `zpt service install` с systemd на реальном Linux (с 0.3.5);
- [x] DNS-сервер комнаты с именами — в CI на Linux (с 0.4.0);
- [ ] резолв имён `*.zpt` через systemd-resolved и NRPT на реальных машинах (с 0.4.0);
- [x] exit через встроенный NAT и лимиты скорости (nftables и NAT) — в CI на Linux (с 0.3.6);
- [ ] exit-узел на Windows на реальной машине (с 0.3.6);
- [ ] VLESS + REALITY в реальных сетях с блокировками и со сторонними серверами VLESS (с 0.2.2);
- [x] определение типа NAT за конусным и симметричным NAT — в CI на NAT-стенде (Linux nftables); с домашними роутерами — не проверялось.

---

## 0.1 — Основа

### 0.1.0 — первый выпуск (тег без сборки, см. 0.1.1)
Всё, что уже есть в коде (фазы 0 и 1 плана):
- туннель AmneziaWG 3: шифрование + PSK комнаты + маскировка от DPI; все комнаты через один UDP-порт со скрытой меткой комнаты;
- контроллер с панелью: комнаты, приглашения (лимит, срок, QR), одобрение, блокировка, исключение, IP и теги, пользователи, журнал;
- подписанные конфиги комнат с закреплением ключа из приглашения; ответы контроллера зашифрованы на ключ узла;
- CLI: `keygen`, `join`, `leave`, `up`, `controller serve|useradd|passwd`, статические комнаты.

Что сделать для выпуска:
- [x] лицензия: узел — MPL-2.0, контроллер — AGPL-3.0 ([LICENSING.md](../LICENSING.md)); DCO для вклада;
- [x] полные тексты лицензий в `LICENSE` и `LICENSE.AGPL`;
- [x] CI зелёный на GitHub (Linux, Windows, macOS; с -race на Linux и macOS);
- [x] goreleaser: бинарники Windows/Linux amd64/arm64, контрольные суммы, заметки к релизу из CHANGELOG, выпуск по тегу ([RELEASING.md](../RELEASING.md));
- [x] политика безопасности ([SECURITY.md](../SECURITY.md));
- [ ] ~~ручная проверка: Windows ↔ Linux в одной LAN~~ → в «Требует ручной проверки».

> Ограничение: устройства должны видеть друг друга напрямую (одна сеть, публичный IP или проброшенный UDP-порт).

### 0.1.1 — срочное исправление ✅ 2026-09-26
- встречные рукопожатия при знакомстве узлов задерживали связь на 5–15 с — исправлено; первый опубликованный выпуск.

### 0.1.2 — Windows без ручной настройки ✅ 2026-09-26
- [x] профиль сети «Частная» для интерфейсов комнат (иначе брандмауэр режет ping/SMB);
- [x] права на файл ключа и `state.json`: только SYSTEM, администраторы и владелец;
- [x] `wintun.dll` в архиве релиза;
- [x] понятная ошибка при запуске `zpt up` без прав администратора.

### 0.1.3 — контроллер в Docker ✅ 2026-09-26
- [x] образ контроллера собирается в CI и публикуется в GitHub Container Registry;
- [x] docker-compose с Caddy (автоматический HTTPS) проверяется в CI для `localhost`.

---

## 0.2 — Связь отовсюду

### 0.2.0 — узел знает свой адрес ✅ 2026-09-26
- [x] STUN (пока в контроллере; в relay — вместе с 0.2.2), определение внешнего адреса и типа NAT;
- [x] UPnP / NAT-PMP: автоматический проброс порта на роутере;
- [ ] PCP → перенесено на потом (редко нужен при наличии UPnP и NAT-PMP);
- [x] панель: тип NAT и внешний адрес каждого участника.

### 0.2.1 — пробивка NAT и relay ✅ 2026-09-26
(вышли одним релизом: код пересекается)
- [x] обмен кандидатами и одновременная пробивка (disco), переход на лучший путь;
- [x] NAT-стенд в CI: network namespaces, настоящие TUN, конусный и симметричный NAT;
- [x] relay в контроллере, UDP-режим, пересылает только зашифрованные пакеты и только между участниками общей комнаты;
- [x] автоматический переход relay → прямое соединение;
- [x] панель: сколько пиров участник видит напрямую и через relay;
- [ ] отдельный `zpt relay` и `MemberCert` для него → позже (сейчас relay знает узлы из базы контроллера);
- [ ] RTT и список relay-серверов в панели → позже.

### 0.2.2 — VLESS + Reality ✅ 2026-09-26
- [x] транспорт до relay по TCP/443, когда UDP заблокирован или режется;
- [x] автопереключение UDP → VLESS (и обратно, когда UDP снова отвечает); внутри остаётся сквозное шифрование AWG;
- [x] исправление 0.2.1: быстрый переход с relay на прямой путь.

### 0.2.3 — настоящая «локалка» ✅ 2026-09-26
- [x] broadcast и multicast (LAN-игры, mDNS, SSDP) с ограничением частоты;
- [x] настройка комнаты: broadcast вкл / выкл / только mDNS.

**Итог вехи:** любые пары типов NAT соединяются (симметричный ↔ симметричный — через relay); при заблокированном UDP связь идёт через VLESS; LAN-игра с поиском по broadcast работает между Windows и Linux через интернет.

---

## 0.3 — Интернет через комнату

### 0.3.0 — доступ к сети за узлом (subnet router) ✅ 2026-09-26
- [x] узел объявляет свою сеть (например, `192.168.1.0/24`), админ одобряет в панели или CLI;
- [x] участники видят устройства этой сети; конфликты адресов обнаруживаются;
- [ ] роутер на Windows и macOS → позже (сейчас только Linux).

### 0.3.1 — exit-узел на Linux ✅ 2026-09-26
- [x] объявление exit → одобрение → выбор на клиенте (CLI и панель);
- [x] клиент на Linux: весь трафик через exit (policy routing);
- [x] локальная сеть exit-узла закрыта для участников.

### 0.3.2 — exit на клиенте Windows ✅ 2026-09-26
- [x] маршруты через интерфейс комнаты; вместо исключений для relay и пиров собственные сокеты узла привязаны к физическому интерфейсу;
- [x] DNS через exit (форвардер на exit-узле), защита от утечек DNS и IPv6 (Linux и Windows).

### 0.3.3 — kill switch ✅ 2026-09-26
- [x] блокировка интернета при потере exit (WFP / nftables);
- [x] опция «оставить доступ к своей LAN».

### 0.3.4 — свой DNS-сервер ✅ 2026-09-26
(по запросу пользователя от 2026-09-26; вместе с ним — руководство [GUIDE.md](GUIDE.md) со скриншотами)
- [x] свой DNS-сервер вне сети (например, свой сервер в интернете) и внутри неё (участник комнаты или устройство в сети за узлом, например Pi-hole / AdGuard Home дома);
- [x] задаётся админом для комнаты в панели (в подписанном конфиге) или на самом устройстве (`zpt dns`), выбор на устройстве важнее;
- [x] работает вместе с exit: внешний DNS-сервер достигается через exit, а exit-узлу можно задать свои вышестоящие серверы.

### 0.3.5 — управление из терминала ✅ 2026-09-26
(по запросу пользователя от 2026-09-26: Linux — полностью, Windows — насколько позволяет система)
- [x] локальный канал управления с работающим узлом (Unix-сокет на Linux, именованный канал на Windows) с доступом только для root / администраторов;
- [x] `zpt status`, `zpt rooms`, `zpt peers` (прямое / relay, RTT, трафик, рукопожатия), `zpt exit` и `zpt dns` применяются сразу, без ожидания;
- [x] служба: `zpt service install|uninstall|start|stop|status` — systemd на Linux, служба Windows; журнал — `zpt logs` (journald на Linux);
- [x] `zpt-controller` из терминала — всё, что есть в панели: участники (список, одобрение, блокировка, исключение, теги, IP), приглашения (список, отзыв), пользователи, настройки комнаты, журнал;
- [x] вывод для скриптов: `--json` у команд просмотра.
(поглощает `zpt status` из 0.4.0)

### 0.3.6 — exit-узел на Windows и лимиты ✅ 2026-09-26
- [x] userspace NAT на gVisor (Windows; на Linux — `exit_nat: userspace`);
- [x] ограничение скорости на exit (на клиента и общее, в каждую сторону): nftables на Linux, в самом NAT — в режиме userspace.

**Итог вехи:** `curl ifconfig.me` показывает IP exit-узла; тест на утечку DNS чистый; при отключении exit интернет блокируется и восстанавливается ≤ 10 с; из комнаты открывается веб-интерфейс домашнего роутера.

---

## 0.4 — Порядок в сети

(`zpt status` и `zpt peers`, задуманные как 0.4.0, вышли в 0.3.5 вместе с полным управлением из терминала; номера вехи сдвинуты.)

### 0.4.0 — DNS-имена ✅ 2026-09-26
- [x] `имя.комната.zpt` у всех участников: DNS-сервер комнаты на адресе узла, NRPT на Windows, systemd-resolved на Linux;
- [x] сервер комнаты не открытый резолвер: чужие имена пересылает только для своей машины и для пользователей exit.

### 0.4.1 — правила доступа (ACL) ✅ 2026-09-26
- [x] правила по тегам, именам, сетям и портам, редактор в панели и CLI, проверка «что если».

### 0.4.2 — надёжность контроллера
- ключи подписи комнат зашифрованы (KEK), экспорт и бэкап;
- миграции схемы между версиями; Postgres как опция.

**Итог вехи:** правило «гости → только игровой сервер» отсекает остальное; имена резолвятся у всех; обновление контроллера со всех 0.3.x без потери данных.

---

## 0.5 — Без центра

### 0.5.0 — gossip
- участники передают друг другу свежие конфиги комнат и списки отзыва.

### 0.5.1 — DHT
- поиск пиров без контроллера, публичный список bootstrap-узлов.

### 0.5.2 — контроллер в клиенте
- `zpt room create --local`: комната без сервера, со-админы с репликацией, продление сертификатов любым со-админом.

**Итог вехи:** контроллер выключен 7 суток — комнаты работают, новые адреса пиров находятся через DHT, сертификаты продлеваются.

---

## 0.6 — Для людей

### 0.6.0 — установщики
- MSI (служба + Wintun), deb/rpm; обработчик ссылок `zpt://`.

### 0.6.1 — трей-клиент
- комнаты с переключателями, пиры, «скопировать IP», выбор exit, вставка приглашения.

### 0.6.2 — автообновление
- подписанные релизы, обновление клиента по каналу stable/beta.

### 0.6.3 — панель для команд
- OIDC, TOTP 2FA, квоты, граф топологии, дашборд (онлайн, % прямых соединений, трафик relay/exit).

### 0.6.4 — документация
- self-host за 10 минут, руководство админа комнаты и пользователя.

**Итог вехи:** новый пользователь на чистой Windows ставит MSI, открывает ссылку-приглашение и не больше чем за 2 минуты играет в LAN-игру и выходит в интернет через exit.

---

## 1.0 — Стабильная

- **1.0.0-rc.1, rc.2, …** — кандидаты: только исправления, никаких новых функций;
  - протокол узел ↔ контроллер и формат на проводе заморожены;
  - AmneziaWG — на релизном теге AWG 3.x вместо master-коммита;
  - внешний аудит безопасности или полноценный threat model review, исправление находок;
  - нагрузка: 250 узлов в комнате, 50 комнат на узле, скорость прямого соединения ≥ 80 % от голого AWG;
  - проверенные обновления со всех 0.6.x.
- **1.0.0** — последний rc без найденных проблем в течение 2 недель.

---

## После 1.0

| Версия | Что |
|---|---|
| 1.1.0 | macOS (utun; Network Extension для App Store) |
| 1.2.0 | Android (VpnService) |
| 1.3.0 | iOS (NetworkExtension) |
| 1.4.0 | Режим L2 (TAP) для не-IP протоколов, мост комнаты в физическую LAN |

Каждая из них тоже будет разбита на патч-релизы. Порядок может меняться по запросам пользователей.

---

## Правила версий и релизов

- **Каждая версия x.y.z — отдельный релиз:** git-тег `vX.Y.Z`, бинарники через goreleaser, контрольные суммы, запись в [CHANGELOG.md](../CHANGELOG.md), заметки к выпуску на русском.
- **Третья цифра (z)** — очередной шаг вехи: небольшая функция и/или исправления. Исправление найденной ошибки выходит в ближайшем следующем z-релизе; если ошибка серьёзная (безопасность, потеря связи), срочный z-релиз выходит сразу, а плановые номера сдвигаются. Номера в этом документе — порядок шагов, а не обещание точного номера.
- **Вторая цифра (y)** — веха. До 1.0 новая веха может ломать совместимость — только с описанием миграции в заметках к релизу. После 1.0 ломающие изменения — только в новой мажорной версии.
- **Совместимость узла и контроллера.** Внутри одной вехи (любые z) — полная. С 0.2 контроллер поддерживает узлы текущей и предыдущей вехи, чтобы узлы можно было обновлять постепенно. Версия протокола — в пути API (`/api/v1/...`).
- **Готовность релиза.** Релиз выходит, только когда: CI зелёный на всех платформах; сквозные тесты проходят; пункты релиза проверены (автотестом или на ручном стенде — что именно, пишется в заметках). Невыполненный пункт переносится в следующий z-релиз, а не выпускается «как есть».
- **Предварительные сборки** между релизами — с суффиксом `-dev` (например `0.1.1-dev`), не публикуются как релизы.
